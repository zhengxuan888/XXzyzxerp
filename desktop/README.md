# 择优臻选 ERP Windows 客户端

该客户端是现有 ERP 的桌面外壳，默认连接 `http://43.134.91.39`，不包含或复制服务器业务数据。

- 本地调试：`pnpm run desktop:start`
- 构建安装程序：`pnpm run desktop:build`
- 临时切换地址：设置环境变量 `ERP_DESKTOP_URL`

安装程序支持选择安装目录，并创建桌面和开始菜单快捷方式。
